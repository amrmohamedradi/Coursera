# Byway LMS API — API_CONTRACT

This file is a literal, code-driven API contract for the Byway (Coursera) backend. It was generated by reading the actual controllers, DTOs, enums, exception types and the global exception middleware. Use this file as the single source of truth for frontend integration.

Important notes (literal)
- Successful responses use the `ApiResponse<T>` envelope implemented in `Coursera.Application.Common.Models.ApiResponse<T>`.
  - Shape (exact JSON):
    ```json
    {
      "success": true,
      "message": null,
      "data": { ... }
    }
    ```
- Global error handling middleware (`Coursera.Api.Middlewares.ExceptionMiddleware`) serializes exceptions as a JSON object with a single property `message` and sets status code based on exception type.
  - Error shape (exact JSON):
    ```json
    { "message": "<error message text>" }
    ```
  - Exception -> HTTP status mapping (exact logic):
    - `ValidationException` (type: `Coursera.Application.Common.Exceptions.ValidationException`) => 400 Bad Request
    - `UnauthorizedException` (type: `Coursera.Application.Common.Exceptions.UnauthorizedException`) => 401 Unauthorized
    - `NotFoundException` (type: `Coursera.Application.Common.Exceptions.NotFoundException`) => 404 Not Found
    - Any other exception type (including `System.UnauthorizedAccessException`) => 500 Internal Server Error
- Because controllers sometimes throw `System.UnauthorizedAccessException` (see Cart / Order controllers when claim not found), those cases will produce 500 with JSON `{"message":"UserId not found in token"}` unless the controller or pipeline maps it otherwise.
- Enums are used in request/response types. Read the Enums section for exact values and how to serialize them (literal behavior is stated there).

---

CONTENTS
- Enums (exact definitions)
- Global success & error shapes (literal)
- Controllers & endpoints (every endpoint): method, route, auth/role, request body (fields & types), success response shape, possible error responses with exact status codes and exact message text and the scenario that produces them.

---

## ENUMS (literal from source)

### `Level` (in `Coursera.Domain.Enums.Level`)

```csharp
public enum Level
{
    AllLevel = 1,
    Beginner,
    Intermediate,
    Export
}
```

- Integer values / names (exact):
  - `1` — `AllLevel`
  - `2` — `Beginner`
  - `3` — `Intermediate`
  - `4` — `Export`
- Used by: `CreateCourseRequest.Level` and `UpdateCourseRequest.Level` (both properties are typed `Level`).
- How to send in request: the server uses default System.Text.Json settings (no custom JSON options in `Program.cs`), so enums are expected/serialized as integers by default. Therefore send the `Level` field as an integer (e.g., `2` for `Beginner`).
- How to interpret in responses: course DTO `CourseDto` has `Level` typed as `Level`; the JSON produced by default will be the numeric value (e.g., `2`).

### `JobTitle` (in `Coursera.Domain.Enums.JobTitle`)

```csharp
public enum JobTitle
{
    FullStackDeveloper = 1,
    BackEndDeveloper,
    FrontEndDeveloper,
    UXUIDesigner
}
```

- Integer values / names (exact):
  - `1` — `FullStackDeveloper`
  - `2` — `BackEndDeveloper`
  - `3` — `FrontEndDeveloper`
  - `4` — `UXUIDesigner`
- How it is used by the API:
  - `InstructorDto` response contains `JobTitle` as an enum typed property. The code's DTO `InstructorDto` uses `JobTitle` type; responses will serialize the enum as an integer by default.
  - When creating/updating an instructor, the controller expects the client to send `jobTitle` as a string (see `CreateInstructorRequest.JobTitle` and `UpdateInstructorRequest.JobTitle` are both `string` types). The controller then calls `Enum.Parse<JobTitle>(request.JobTitle)` — i.e., it expects the enum *name* (e.g., `"BackEndDeveloper"`) in the request body.
- Summary: responses (Instructor DTO) will have numeric enum values; requests for creating/updating instructors require the job title string name.

### Category
- There is no `Category` enum type in code. Category is a database entity (`Coursera.Domain.Entities.Category`). Categories are returned as `CategoryDto` objects.

---

## GLOBAL RESPONSE SHAPES (literal)

Success envelope (exact):

```json
{
  "success": true,
  "message": null,
  "data": { ... }
}
```

Error shape produced by the global exception middleware (exact):

```json
{ "message": "<literal exception message text>" }
```

Notes about status codes the middleware sets (literal mapping):
- `ValidationException` => 400
- `UnauthorizedException` => 401
- `NotFoundException` => 404
- all other exceptions => 500

Because some code throws `System.UnauthorizedAccessException` (controllers that fail to find the user id claim), those will be treated as "other exceptions" and return 500 with the `message` text from the thrown exception.

---

## CONTROLLERS & ENDPOINTS (every controller found in source)

For each endpoint we list:
- HTTP method, Route (literal), Authentication requirement (literal: none / authenticated / Admin role), Request body with exact C# type & JSON field names, Success response (literal JSON envelope and `data` shape), and Error responses (exact status code, exact JSON returned and the exact code string that results in that error).


---

### AUTH - `Coursera.Api.Controllers.AuthController`

Base path: `/api/auth`

#### POST `/api/auth/register`
- Auth: none
- Request body type: `RegisterCommand` (in `Coursera.Application.Features.Auth.Register.RegisterCommand`)
  - JSON fields (exact):
    - `firstName`: string
    - `lastName`: string
    - `email`: string
    - `password`: string
- Success response (HTTP 200) — exact JSON:
  ```json
  {
    "success": true,
    "message": null,
    "data": {
      "token": "<string>",
      "refreshToken": "<string>",
      "email": "<string>"
    }
  }
  ```
- Error responses (literal):
  - `400 Bad Request` with body `{ "message": "<message>" }` when a `ValidationException` is thrown. Scenarios:
    - The identity creation failed: `AuthService.RegisterAsync` throws `ValidationException` with message containing the joined identity error descriptions (literal text is the joined descriptions returned by ASP.NET Identity). Example message could be: `"Passwords must have at least one non alphanumeric character."` (the middleware will return that exact text in the `message` field).

#### POST `/api/auth/login`
- Auth: none
- Request body type: `LoginCommand`
  - JSON fields: `email` (string), `password` (string)
- Success response (200) — same envelope as register; `data` contains `{ token, refreshToken, email }`.
- Error responses:
  - `401 Unauthorized` with body `{ "message": "Invalid email or password." }` — thrown by `AuthService.LoginAsync` when email not found or password incorrect (literal message text: `Invalid email or password.`).

#### POST `/api/auth/refresh`
- Auth: none
- Request body type: `RefreshTokenCommand`
  - JSON fields: `email` (string), `refreshToken` (string)
- Success response (200) — `data` same as login/register (new token/refreshToken/email).
- Error responses (literal):
  - `401 Unauthorized` with `{ "message": "Invalid refresh token." }` — when no user exists with the provided email (literal message from `AuthService.RefreshTokenAsync`).
  - `401 Unauthorized` with `{ "message": "Invalid or expired refresh token." }` — when provided refresh token is missing or not active (literal message from `AuthService.RefreshTokenAsync`).

---

### CART - `Coursera.Api.Controllers.CartController`

Base path: `/api/cart`

> The controller reads the current user id from the JWT claim `ClaimTypes.NameIdentifier` inside the action. If that claim is missing the code throws `System.UnauthorizedAccessException` with message `"UserId not found in token"`.

#### GET `/api/cart`
- Auth: authenticated user required (no role required)
- Request body: none
- Success response (200) — exact shape (envelope) with `data` a `CartDto` (`Coursera.Application.Common.DTOs.CartDto`):
  ```json
  {
    "success": true,
    "message": null,
    "data": {
      "courses": [
        {
          "courseId": "<guid>",
          "description": "<string>",
          "price": <decimal>,
          "imagePath": "<string>"
        }
      ],
      "subtotal": <decimal>,
      "total": <decimal>,
      "tax": <decimal>
    }
  }
  ```
- Error responses (literal):
  - If the claim is missing, the controller throws `System.UnauthorizedAccessException` with message `UserId not found in token`. The global middleware returns HTTP `500 Internal Server Error` with body `{ "message": "UserId not found in token" }` (note: this is a 500 because the middleware only maps the custom `UnauthorizedException` to 401).

#### DELETE `/api/cart/{courseId}`
- Auth: authenticated user required
- Route param: `courseId` (guid)
- Request body: none
- Success response (200) — envelope with `data` = null:
  ```json
  { "success": true, "message": null, "data": null }
  ```
- Error responses (literal):
  - If the user id claim is missing: `500` with `{ "message": "UserId not found in token" }` (same reason as GET).
  - Other domain errors thrown by command handlers would be returned as `{ "message": "<text>" }` and status code according to exception type thrown in the handler.

---

### ORDER - `Coursera.Api.Controllers.OrderController`

Base path: `/api/order`

#### POST `/api/order/checkout`
- Auth: authenticated user required
- Request body: none (user id extracted from JWT claim)
- Success response (200) — `data` is a GUID (order id):
  ```json
  { "success": true, "message": null, "data": "<order-guid>" }
  ```
- Error responses (literal):
  - If user id claim missing in token: controller throws `System.UnauthorizedAccessException` with message `UserId not found in token` → middleware returns `500` and `{ "message": "UserId not found in token" }`.
  - Checkout command handlers may throw other exceptions (e.g., `ValidationException`) — those will be returned as `{ "message": "<literal message>" }` with the HTTP status mapped by the middleware.

#### GET `/api/order/Seccess`
- Auth: none
- Success response (200):
  ```json
  {
    "success": true,
    "message": null,
    "data": {
      "message": "Payment completed successfully"
    }
  }
  ```

---

### COURSE - `Coursera.Api.Controllers.CourseController`

Base path: `/api/course`

#### GET `/api/course/{id}`
- Auth: none
- Route param: `id` (guid)
- Success response (200) — `data` is `CourseDto` (exact `CourseDto` record fields):
  ```json
  {
    "id": "<guid>",
    "name": "<string>",
    "description": "<string>",
    "price": <decimal>,
    "rating": <decimal>,
    "createdAt": "<ISO date string>",
    "level": <integer>,
    "imagePath": "<string>",
    "categoryId": "<guid>",
    "instructorId": "<guid>"
  }
  ```
  - Note: `level` is the numeric value from `Level` enum.
- Error responses:
  - If handler throws `NotFoundException` with message `Course not found` → middleware returns `404` with `{ "message": "Course not found" }`.

#### GET `/api/course` (list)
- Auth: none
- Query params: `pageNumber` (int, default 1), `pageSize` (int, default 10), `search` (string, optional)
- Success response (200) — `data` is `PaginatedList<CourseDto>` (exact shape used by the application). The project uses a paginated wrapper; typical JSON shape in `API_DOCUMENTATION.md`:
  ```json
  {
    "items": [ { /* CourseDto */ } ],
    "totalCount": <int>,
    "pageNumber": <int>,
    "pageSize": <int>
  }
  ```
- Error responses: handler may throw exceptions that result in `{ "message": "<text>" }` as described in global middleware.

#### POST `/api/course`
- Auth: Admin role required (`[Authorize(Roles = Roles.Admin)]`)
- Request body type: `CreateCourseRequest` (`Coursera.Application.Common.Models.CreateCourseRequest`)
  - JSON fields (exact):
    - `name`: string
    - `description`: string
    - `price`: decimal
    - `rating` (or named `rate` in command): decimal
    - `imagePath`: string
    - `createdAt`: ISO 8601 datetime string
    - `level`: integer (use `Level` numeric value, e.g., `2` for `Beginner`)
    - `categoryId`: guid
    - `instructorId`: guid
- Success response (200): `data` contains `Guid` (created course id):
  ```json
  { "success": true, "message": null, "data": "<created-course-guid>" }
  ```
- Error responses (literal):
  - If validation on request fails and a `ValidationException` is thrown by the handler: `400` with `{ "message": "<literal validation message>" }`.
  - If user is authenticated but not in `Admin` role: ASP.NET authorization will return `403 Forbidden` (default framework behavior) — note: middleware does not alter framework authorization responses.

#### PUT `/api/course/{id}`
- Auth: Admin role required
- Request body type: `UpdateCourseRequest` (same fields as create; `level` is enum `Level` — send integer)
- Success response (200) — envelope with `data` = null: `{ "success": true, "message": null, "data": null }`.
- Error responses:
  - If course not found: handler throws `NotFoundException("Course not found")` → `404` with `{ "message": "Course not found" }`.
  - If validation fails: `400` with `{ "message": "<text>" }`.

#### DELETE `/api/course/{id}`
- Auth: Admin role required
- Success response (200) — envelope with `data` = null.
- Error responses (literal & exact messages from handlers):
  - `404 Not Found` with `{ "message": "Course not found" }` — when course with given id does not exist (handler throws `NotFoundException("Course not found")`).
  - `400 Bad Request` with `{ "message": "Cannot delete course because it was purchased" }` — when `_context.OrderItems.AnyAsync(oi => oi.CourseId == id)` is true and the delete handler throws `ValidationException("Cannot delete course because it was purchased")`.

---

### CATEGORY - `Coursera.Api.Controllers.CategoryController`

Base path: `/api/category`

#### GET `/api/category/{id}`
- Auth: none
- Success response (200): `data` is `CategoryDto` (fields: `id`, `name`, `imagePath`).
- Error responses:
  - `404 Not Found` with `{ "message": "Category not found" }` — thrown by `GetcategoryByIdHandler`.

#### GET `/api/category`
- Auth: none
- Success response: `data` is `List<CategoryDto>` — each `CategoryDto` has `{ id, name, imagePath }`.

#### POST `/api/category`
- Auth: Admin required
- Request body type: `CreateCategoryRequest` (fields `name` string, `imagePath` string)
- Success response: `200` with `data` containing created category id (GUID)
- Error responses:
  - `400` (ValidationException) — if handler or validators throw `ValidationException`, middleware returns `{ "message": "<text>" }`.

#### PUT `/api/category/{id}`
- Auth: Admin required
- Request body type: `UpdateCategoryRequest` (fields `name`, `imagePath`)
- Success response: `200` with `data` = null
- Error responses:
  - `404 Not Found` with `{ "message": "Category not found" }` — if the category to update does not exist and a `NotFoundException` is thrown by handler.

#### DELETE `/api/category/{id}`
- Auth: Admin required
- Success response: `200` with `data` = null
- Error responses (literal):
  - `404 Not Found` with `{ "message": "Category not found" }` — when category missing.
  - `400 Bad Request` with `{ "message": "Cannot delete category with courses" }` — when `_context.Courses.AnyAsync(c => c.CategoryId == id)` is true and handler throws `ValidationException("Cannot delete category with courses")`.

---

### INSTRUCTOR - `Coursera.Api.Controllers.InstructorController`

Base path: `/api/instructor`
- The controller has `[Authorize(Roles = Roles.Admin)]` on the controller class; all actions require Admin role.

#### GET `/api/instructor/{id}`
- Auth: Admin only
- Success response: `data` is `InstructorDto` record:
  ```json
  {
    "id": "<guid>",
    "name": "<string>",
    "jobTitle": <integer>,
    "bio": "<string>",
    "imagePath": "<string>"
  }
  ```
  - `jobTitle` is numeric value of `JobTitle` enum in responses.
- Error responses:
  - `404` with `{ "message": "Instructor not found" }` when missing.

#### GET `/api/instructor` (list)
- Auth: Admin only
- Query params: `pageNumber`, `pageSize`, `search` (optional)
- Success response: paginated list of `InstructorDto` items.

#### POST `/api/instructor`
- Auth: Admin only
- Request body: `CreateInstructorRequest` (JSON fields):
  - `name`: string
  - `jobTitle`: string (literal enum name expected, e.g., `"BackEndDeveloper"`)
  - `bio`: string
  - `imagePath`: string
- The controller parses `jobTitle` using `Enum.Parse<JobTitle>(request.JobTitle)`. If the provided string is not a valid enum name, model binding or the handler will fail (likely result in `400`/`500` depending on where parsing error occurs).
- Success response: `200` with `data` containing created instructor GUID.

#### PUT `/api/instructor/{id}`
- Auth: Admin only
- Request body: `UpdateInstructorRequest` (same fields as create; `jobTitle` string name)
- Success response: `200` with `data` = null
- Error responses:
  - `404` with `{ "message": "Instructor not found" }` if missing.
  - `400` with `{ "message": "Cannot delete instructor with courses" }` is used only in delete handler; updates will surface validation exceptions similarly.

#### DELETE `/api/instructor/{id}`
- Auth: Admin only
- Success response: `200` with `data` = null
- Error responses (literal):
  - `404 Not Found` with `{ "message": "Instructor not found" }` — when no instructor found.
  - `400 Bad Request` with `{ "message": "Cannot delete instructor with courses" }` — when the instructor has courses and handler throws `ValidationException("Cannot delete instructor with courses")`.

---

### HOME - `Coursera.Api.Controllers.HomeController`

Base path: `/api/home`

#### GET `/api/home/top-courses`
- Auth: none
- Success: `200` with `data` = `List<CourseDto>`

#### GET `/api/home/top-Categories`
- Auth: none
- Success: `200` with `data` = `List<CategoryDto>`

#### GET `/api/home/top-Instructor`
- Auth: none
- Success: `200` with `data` = `List<InstructorDto>`

(Note: the names `top-Categories` and `top-Instructor` are literal route paths used in the controller.)

---

### DASHBOARD - `Coursera.Api.Controllers.DashBoardController`

Base path: `/api/dashBoard`
- Controller has `[Authorize(Roles = Roles.Admin)]` — Admin only.

#### GET `/api/dashBoard`
- Auth: Admin only
- Success response (200) — `data` contains dashboard statistics returned by `GetDashbordQuery` (structure is defined in application code). If not found or other errors occur, appropriate exception messages are returned by middleware.

---

### SIMILAR COURSES - `Coursera.Api.Controllers.GetSimilarCoursesController`

Base path: `/api/getSimilarCourses`

#### GET `/api/getSimilarCourses/{id}/similar`
- Auth: none
- Route param: `id` (guid)
- Success response: `200` with `data` being `List<CourseDto>` (similar courses list)
- Error responses: typical `404` or `400` depending on handler; middleware returns `{ "message": "<text>" }`.

---

## LITERAL EXCEPTION MESSAGES (pulled directly from handlers & services)

Include these exact messages when checking error responses from the server. When the middleware returns `{ "message": "<text>" }`, the `<text>` will be one of the following literals depending on where it originated:

- "Category not found"
- "Cannot delete category with courses"
- "Course not found"
- "Cannot delete course because it was purchased"
- "Instructor not found"
- "Cannot delete instructor with courses"
- "Invalid email or password."  (note the trailing period)
- "User not found."
- "Invalid refresh token."
- "Invalid or expired refresh token."
- "UserId not found in token" (thrown by controllers when claim missing; note this is `System.UnauthorizedAccessException`, so middleware will return HTTP 500 but the `message` value will equal this literal)
- Identity error messages joined into a single string — produced by `AuthService.RegisterAsync` when identity creation fails (literal message is the string-joined descriptions provided by ASP.NET Identity errors; the exact text depends on Identity configuration and the failing rule).

---

## SERIALIZATION/CONSUMPTION RULES (literal behavior found in code)

- Enums:
  - `Level` and `JobTitle` are defined as enums. By default the API uses System.Text.Json default settings, which serialize enums as integers. Therefore expect numeric values in responses for enum-typed DTO properties.
  - For creating/updating instructors, the controller expects the `jobTitle` field to be a string containing the enum *name* (e.g., `"BackEndDeveloper"`) because the request DTO uses `string` and the controller calls `Enum.Parse<JobTitle>(request.JobTitle)`.
- Auth:
  - Obtain `token` and `refreshToken` from `/api/auth/login` or `/api/auth/register`.
  - For protected endpoints include header: `Authorization: Bearer <token>`
- CORS: the project defines a permissive policy named `AllowAll` in `Program.cs`, but `app.UseCors("AllowAll")` is not present in the running pipeline. Until `UseCors` is added, browsers may be blocked depending on host configuration.

---

## FAQ / Integration notes (literal)

- How to detect a validation error vs. domain error vs. auth error?
  - Validation errors thrown using the project's `ValidationException` appear with status `400` and body `{ "message": "<text>" }`.
  - Not found situations throw `NotFoundException` and return `404` with `{ "message": "<text>" }`.
  - Authentication failures thrown using `UnauthorizedException` (project custom type) return `401` with `{ "message": "<text>" }`.
  - If a controller throws `System.UnauthorizedAccessException` (this project does in claim extraction), it will be treated as an "other" exception and return `500` with `{ "message": "<text>" }`.

- Should frontend rely on HTTP 403 for non-admin calls? Yes — ASP.NET authorization will return 403 when the token is valid but lacks required role. The middleware does not override framework authorization responses.

- Use the literal route names and parameter names exactly as shown above. Some route names are capitalized (e.g., `/api/order/Seccess`) — use the exact path.

---

If you want, I can generate a Postman collection or a CSV table listing all endpoints with example request bodies and example success/error responses. Which would you like next?